import { createTheme } from '@mui/material/styles';

const customTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: '#d3454f;',
    },
  },
});

export default customTheme;