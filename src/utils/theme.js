import { createTheme } from '@mui/material/styles';

const customTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: '#20b2aa',
    },
  },
});

export default customTheme;